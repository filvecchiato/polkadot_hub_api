{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": [".papi/metadata/*", ".papi/polkadot-api.json", "package.json"],
      "outputs": [".papi/descriptors"]
    }
  }
}
